const CACHE='mitsumori-public-demo-v1.2.3';const scopeUrl=new URL(self.registration.scope);const asset=(path='')=>new URL(path,scopeUrl).toString();const SHELL=[asset(''),asset('index.html'),asset('manifest.webmanifest'),asset('icons/icon-192.png')];self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)));self.skipWaiting()});self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))));self.clients.claim()});self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==self.location.origin)return;if(url.pathname.includes('/api/')||url.pathname.includes('/data/'))return;event.respondWith(fetch(req).then(res=>{const copy=res.clone();if(res.ok&&['document','script','style','image','manifest'].includes(req.destination)){caches.open(CACHE).then(cache=>cache.put(req,copy))}return res}).catch(()=>caches.match(req).then(cached=>cached||caches.match(asset('index.html')))))});
